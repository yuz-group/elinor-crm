# Agent Instructions

- Do not commit application code, demo files, generated files, caches, databases, backups, ZIP archives, or other binary artifacts unless explicitly requested.
- Keep empty repository directories tracked with a `.gitkeep` file.
- Follow the existing project structure when adding new files.
